
datasets:
  # Stage 4 single-sample test config
  # Verify that the training pipeline runs end-to-end
  
  # Use only 1 sample for testing
  - json_path: data_config/social_iq_v2_rewrite.json
    data_root: ${PROJECT_ROOT}/data/videos
    sampling_strategy: first:1
