
datasets:
  # Stage 4: API Reward test (only 3 samples)
  
  # 1. Social-IQ (random 3 samples)
  - json_path: data_config/social_iq_v2_rewrite.json
    data_root: ${PROJECT_ROOT}/data/IntentTrain/videos
    sampling_strategy: random:3
